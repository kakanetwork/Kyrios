

// Não faça cache de nenhum arquivo
// Defina o nome do cache como vazio para evitar armazenamento em cache
var staticCacheName = '';

// Não faça nada no evento "install"
self.addEventListener("install", event => {
});

// Não faça nada no evento "activate"
self.addEventListener('activate', event => {
});

// Não faça nada no evento "fetch"
self.addEventListener("fetch", function (event) { 
});
